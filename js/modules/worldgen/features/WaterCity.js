// js/modules/worldgen/features/WaterCity.js

export class WaterCity {
  constructor(world, physicsWorld) {
      this.world = world;
      this.physicsWorld = physicsWorld;
  }

  generateNearSpring(springX, springZ) {
      if (Math.random() < 0.5) { // 50%の確率で生成
          // 水の都市の構造物を生成
      }
  }
}