// js/modules/worldgen/features/CivilizationRemnant.js

export class CivilizationRemnant {
    constructor(world, physicsWorld) {
        this.world = world;
        this.physicsWorld = physicsWorld;
    }

    generateNearLargeRiver(largeRiverX, largeRiverZ) {
        if (Math.random() < 0.5) { // 50%の確率で生成
            // 欠片の構造物を生成
        }
    }
}